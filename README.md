# Habit Tracker
A simple command-line tool to track streaks for daily habits. It persists your data locally in a JSON file.

## Usage
- Check in a habit: `habit <habit_name>`
- Example: `habit reading` (This will create or update the streak for "reading").